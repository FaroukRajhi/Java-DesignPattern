Provides us with a uniform way to access different collections of objects.
If we get an array, arrayList and Hashtable of objects, we pop out an iterator for each and treat theam the same.
This provides a uniform way to cycle through different collections.

=> The Iterator pattern provides a way to access elements of a collection sequentially without exposing its underlying representation.

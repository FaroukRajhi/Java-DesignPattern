Allows to modify an aobject dynamically.
We should use it when we want the capabalities of inheritance with subclasses, but we need to add functionality rt run-time.
It is more flexible than inheritance.
Simplifies code beacause we add functionality using many simple classes.

=> allows behavior to be added to individual objects dynamically, 
without affecting the behavior of other objects from the same class.

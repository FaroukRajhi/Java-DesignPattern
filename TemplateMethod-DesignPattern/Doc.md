Used to create a group of subclasses that have to execute a similar group of methods.
We create an abstract class that contains a method called the Template Method.

 It lets subclasses redefine certain steps without changing the algorithm's structure.
